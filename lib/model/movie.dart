class Movie {
  String? name, image, type,duration, description, url;
  int? vote;
  double? rating;

  Movie(
      {this.name,
      this.image,
      this.type,
      this.rating,
      this.vote,
      this.duration,
      this.description,
      this.url});

}
